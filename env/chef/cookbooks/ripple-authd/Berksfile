site :opscode

metadata

group :test do
  # ubuntu support cookbooks
  cookbook 'apt'
  # centos support cookbooks
  cookbook 'yum'
  # metatest support cookbooks
  cookbook 'minitest-handler'
  # test environment setup cookbook
  cookbook 'ripple-authd-test', :path => './test/cookbook'
end
